{{#if message}}
    <div class="p-4 mb-4 text-sm font-medium rounded-lg {{#if error}}bg-red-100 text-red-800{{else}}bg-green-100 text-green-800{{/if}}" role="alert">
        {{ message }}
    </div>
{{/if}}