
  
  <h1 class="text-2xl font-bold inline-block">Recent</h1>
    <!--This button dispalays a modal which will display any friend requests along with give the ability to send requests to users in database-->

    <div class="grid grid-cols-3 gap-8 mt-6">
      <div class="col-span-2 bg-white p-4 rounded-md shadow-md">
      <!--This is where friend activities will be displayed once friend functionality is working, both of these fields will work the same-->
        <h2 class="text-xl font-semibold mb-4">Activity</h2>
        <div class="space-y-4">
        {{#each activities}}  
          <div class="flex justify-between items-center p-3 bg-gray-100 rounded-md">
            
            <!-- LEFT: User + Notes + Duration -->
            <div class="text-base">

              <div class="flex items-center gap-3">
                <img src={{#if profile_photo_path}} {{profile_photo_path}}{{else}} "../../extra_resources/images/default.png" {{/if}} class="w-10 h-10 object-cover rounded-full">
                <p><strong>{{username}}</strong> - {{activity_name}}</p>
              </div>

              {{#if notes}}<p class="text-sm mt-1 ml-13">{{notes}}</p>{{/if}}
              <p class = "mt-1 ml-13">{{#if distance_mi}}{{distance_mi}} miles • {{/if}}{{#if duration_minutes}}{{duration_minutes}} minute(s){{/if}}</p>
              
            </div>

            <!-- RIGHT: Time + Button -->
            <div class="flex items-center gap-3 text-sm text-gray-600">
              <span>{{formatDate activity_date}} at {{formatTime activity_time}}</span>
              <button class="bg-blue-200 p-2 rounded-full hover:bg-blue-300">
                💬
              </button>
            </div>

          </div>
        {{/each}}
        </div>
      </div>

    <!--This is where friend achievements will be displayed once friend functionality is working, both of these fields will essentially work the same-->
      <div class="bg-white p-4 rounded-md shadow-md">
        <h2 class="text-xl font-semibold mb-4">Achievements</h2>
        <div class="space-y-3">
          {{#each achievements}}
              <div class="flex items-center gap-3">
                <img src={{#if profile_photo_path}} {{profile_photo_path}}{{else}} "../../extra_resources/images/default.png" {{/if}} class="w-10 h-10 object-cover rounded-full">
                <p><strong>{{username}}</strong> - {{achievement_name}}</p>
              </div>
              <p class="text-sm ml-13"> ✅ {{description}}</p>
          {{/each}}
        </div>
      </div>
    </div>
  </main>
</div>



